# Database
DATABASE_URL="postgresql://username:password@localhost:5432/pims_db?schema=public"

# Application
PORT=3000
NODE_ENV=development
JWT_SECRET=change_me

# Remote analytics uploader (optional)
# When all three are set, the cron uploader will POST analytics snapshots every 60s
REMOTE_ANALYTICS_BASE_URL=https://your-api.example.com
REMOTE_ANALYTICS_API_KEY=replace_with_api_key
REMOTE_ANALYTICS_PHARMACY_ID=pharmacy_1
